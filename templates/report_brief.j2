# 🎯 {{ report_date }} {{ labels.brief_title }}

> {{ results|length }} {{ labels.stock_unit_compact }} | 🟢{{ buy_count }} 🟡{{ hold_count }} 🔴{{ sell_count }}

{% for e in enriched %}
{% set dash = e.result.dashboard or {} %}
{% set core = (dash.get('core_conclusion') or {}) if dash else {} %}
{% set one = (core.get('one_sentence') or e.result.analysis_summary or '')[:60] %}
{% set matched = e.result.matched_skills or [] %}
{% set primary = (matched[0].name or matched[0].id) if matched else '' %}
**{{ e.stock_name }}({{ e.result.code }})** {{ e.signal_emoji }} {{ e.localized_operation_advice }} | {{ labels.score_label }} {{ e.result.sentiment_score }}{% if primary %} | 🎯 {{ primary }}{% endif %} | {{ one }}
{% endfor %}

*{{ report_timestamp }}*
